class Student {
    /**
     * @param {number} matrikelnummer
     * @param {string} knummer
     * @constructor
     */
    constructor(matrikelnummer, knummer) {
        this.matrikelnummer = matrikelnummer;
        this.knummer = knummer;
    }
}

module.exports = Student